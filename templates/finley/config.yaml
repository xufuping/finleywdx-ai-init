# Finley 项目基线配置
quality:
  frontend:
    lint: "<占位: npm run lint>"
    typecheck: "<占位: npm run typecheck>"
    test: "<占位: npm test>"
  backend:
    lint: "<占位: ruff check .>"
    typecheck: "<占位: mypy .>"
    test: "<占位: pytest>"

# 留空时 record_work.py 会使用 FINLEY_DEVELOPER、git user.name，再回退至系统用户名。
developer: ""
